SELECT
    record_date,
    col1,
    col2,
    LAG(col1) OVER (ORDER BY record_date) AS prev_col1,
    LAG(col2) OVER (ORDER BY record_date) AS prev_col2
FROM daily_data
WHERE col1 <> LAG(col1) OVER (ORDER BY record_date)
   OR col2 <> LAG(col2) OVER (ORDER BY record_date);