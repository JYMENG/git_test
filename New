Import-Module ActiveDirectory
Import-Module ImportExcel

# Load Excel with columns: GroupName | Server
$groups = Import-Excel -Path "groups.xlsx"
$allUsers = @()

# Recursive function (defined once, outside loop)
function Get-GroupUsersRecursive {
    param (
        [string]$CurrentGroup,
        [string]$Server,
        [hashtable]$seenGroups,
        [string]$ParentGroup
    )

    if ($seenGroups.ContainsKey($CurrentGroup)) { return }
    $seenGroups[$CurrentGroup] = $true

    try {
        $members = Get-ADGroupMember -Identity $CurrentGroup -Server $Server -ErrorAction Stop
    } catch {
        Write-Warning "❌ Cannot get members of $CurrentGroup: $_"
        return
    }

    foreach ($member in $members) {
        $dn = $member.DistinguishedName
        $entry = [PSCustomObject]@{
            RootGroup         = $ParentGroup   # Top-level group from Excel
            ParentGroup       = $CurrentGroup  # This group where the member was found
            Server            = $Server
            Name              = $member.Name
            ObjectClass       = $member.ObjectClass
            SamAccountName    = ''
            Email             = ''
            DistinguishedName = $dn
            Note              = ''
        }

        # Handle external/trusted users
        if ($dn -like '*CN=ForeignSecurityPrincipals*') {
            $entry.Note = "⚠️ External object (ForeignSecurityPrincipal)"
            $script:allUsers += $entry
            continue
        }

        if ($member.objectClass -eq 'user') {
            try {
                $user = Get-ADUser -Identity $dn -Server $Server -Properties EmailAddress
                $entry.SamAccountName = $user.SamAccountName
                $entry.Email = $user.EmailAddress
            } catch {
                $entry.Note = "⚠️ Cannot resolve user"
            }
        } elseif ($member.objectClass -eq 'group') {
            # Recurse into subgroup
            Get-GroupUsersRecursive -CurrentGroup $member.Name -Server $Server -seenGroups $seenGroups -ParentGroup $ParentGroup
            continue
        } else {
            $entry.Note = "⚠️ Unsupported object class: $($member.objectClass)"
        }

        $script:allUsers += $entry
    }
}

# Loop through each root group from Excel
foreach ($row in $groups) {
    $rootGroup = $row.GroupName
    $server = $row.Server

    if (-not $rootGroup -or -not $server) {
        Write-Warning "⚠️ Skipping row with missing GroupName or Server."
        continue
    }

    Write-Host "🔍 Processing group '$rootGroup' on server '$server'"

    $seenGroups = @{}
    Get-GroupUsersRecursive -CurrentGroup $rootGroup -Server $server -seenGroups $seenGroups -ParentGroup $rootGroup
}

# Export to CSV
$allUsers | Export-Csv -Path "AD_Users_Recursive_WithNotes.csv" -NoTypeInformation
Write-Host "✅ Export complete: AD_Users_Recursive_WithNotes.csv"