PivotedTable =
    SUMMARIZE(
        FilteredTable,
        YourTable[RowField],         -- Row field
        YourTable[ColumnField],      -- Column field
        "TotalValue", SUM(YourTable[ValueField])  -- Value to aggregate
    )