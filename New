import pandas as pd
import json

# Load Excel file
df = pd.read_excel("data.xlsx")

# Assume the JSON data is in a column named "json_column"
df["parsed_json"] = df["json_column"].apply(json.loads)

# Now you can access nested data, for example:
print(df["parsed_json"][0])  # First row's parsed JSON