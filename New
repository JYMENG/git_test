Sub ListPivotCacheFields()
    Dim pc As PivotCache
    Dim f As PivotField

    Set pc = ThisWorkbook.PivotCaches.Create( _
        SourceType:=xlDatabase, _
        SourceData:="DataTbl")

    For Each f In pc.PivotFields
        Debug.Print "[" & f.Name & "]"
    Next f
End Sub