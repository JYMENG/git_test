WITH s AS (
  SELECT
    record_date,
    col1, col2,
    LAG(col1) OVER (ORDER BY record_date) AS prev_col1,
    LAG(col2) OVER (ORDER BY record_date) AS prev_col2
  FROM daily_data
)
SELECT record_date, col1, col2
FROM s
WHERE (col1, col2) IS DISTINCT FROM (prev_col1, prev_col2);