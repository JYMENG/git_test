let
    Source = Excel.CurrentWorkbook(){[Name="Table1"]}[Content],
    Grouped = Table.Group(Source, {"Key"}, {
        {"Combined", each Text.Combine([Value], ", "), type text}
    })
in
    Grouped