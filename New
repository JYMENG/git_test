# Install the Graph module (only once)
Install-Module Microsoft.Graph -Scope AllUsers

# Import it into your session
Import-Module Microsoft.Graph

# Connect to Graph
Connect-MgGraph -Scopes "Group.Read.All"