(List.Last(
    List.FirstN(
        List.Select(
            List.Transform(
                {1..[n]*2}, 
                each Date.AddDays([Date], -_)), 
            each Date.DayOfWeek(_, Day.Monday) < 5),
        [n])
))